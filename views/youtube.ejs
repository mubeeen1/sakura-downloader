<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>🌸 YouTube Downloader - Sakura</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🌸</text></svg>" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="/css/styles.css" />
    
    <style>
        /* YouTube-specific custom styles - isolated from other platforms */
        .yt-media-preview-container {
            background: rgba(255, 255, 255, 0.06);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.12);
            border-radius: 24px;
            padding: 2.5rem;
            margin: 2.5rem 0;
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
            position: relative;
            overflow: hidden;
        }

        .yt-media-preview-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, #ff6b6b, #667eea, #764ba2);
        }

        .yt-thumbnail-display {
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .yt-thumbnail-display img {
            max-width: 100%;
            max-height: 300px;
            width: auto;
            height: auto;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
            transition: transform 0.4s ease, box-shadow 0.4s ease;
        }

        .yt-thumbnail-display img:hover {
            transform: scale(1.02);
            box-shadow: 0 12px 48px rgba(0, 0, 0, 0.35);
        }

        .yt-video-info {
            text-align: center;
            margin-bottom: 2rem;
        }

        .yt-video-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #ffffff;
            margin: 0 0 1.2rem 0;
            line-height: 1.4;
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
        }

        .yt-video-details {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            flex-wrap: wrap;
        }

        .yt-video-detail {
            background: rgba(255, 255, 255, 0.08);
            padding: 0.8rem 1.2rem;
            border-radius: 12px;
            color: rgba(255, 255, 255, 0.9);
            font-size: 0.85rem;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .yt-video-detail strong {
            color: #ffffff;
            display: block;
            margin-bottom: 0.2rem;
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .yt-format-options-list {
            display: flex;
            flex-direction: column;
            gap: 0.8rem;
        }

        .yt-format-strap {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.04) 100%);
            border: 1px solid rgba(255, 255, 255, 0.12);
            border-radius: 16px;
            padding: 1.2rem 1.8rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            backdrop-filter: blur(8px);
            position: relative;
            overflow: hidden;
        }

        .yt-format-strap::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .yt-format-strap:hover::before {
            left: 100%;
        }

        .yt-format-strap:hover {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.12) 0%, rgba(255, 255, 255, 0.06) 100%);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.25);
            border-color: rgba(255, 255, 255, 0.2);
        }

        .yt-format-details {
            display: flex;
            flex-direction: column;
            gap: 0.4rem;
        }

        .yt-format-type-quality {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .yt-format-type {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
            color: white;
            padding: 0.3rem 0.8rem;
            border-radius: 8px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            box-shadow: 0 2px 8px rgba(255, 107, 107, 0.3);
        }

        .yt-format-quality {
            color: #ffffff;
            font-weight: 600;
            font-size: 0.9rem;
        }

        .yt-format-size {
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.8rem;
            font-weight: 500;
        }

        .yt-convert-button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 0.9rem 1.8rem;
            border-radius: 50px;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex;
            align-items: center;
            gap: 0.6rem;
            white-space: nowrap;
            position: relative;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .yt-convert-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .yt-convert-button:hover::before {
            left: 100%;
        }

        .yt-convert-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.5);
        }

        .yt-convert-button:active {
            transform: translateY(-1px);
        }

        .yt-convert-button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .yt-reset-button {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
            color: white;
            border: none;
            padding: 1.2rem 2.5rem;
            border-radius: 50px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex;
            align-items: center;
            gap: 0.8rem;
            margin: 2.5rem auto 0;
            width: fit-content;
            box-shadow: 0 4px 15px rgba(255, 107, 107, 0.3);
        }

        .yt-reset-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.5);
        }

        /* Download modal styles */
        .yt-modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.85);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            backdrop-filter: blur(8px);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .yt-modal-overlay.show {
            opacity: 1;
        }

        .yt-download-modal {
            background: rgba(255, 255, 255, 0.12);
            backdrop-filter: blur(25px);
            border: 1px solid rgba(255, 255, 255, 0.25);
            border-radius: 24px;
            padding: 2.5rem;
            max-width: 450px;
            width: 90%;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
            text-align: center;
            color: white;
            position: relative;
            transform: scale(0.9);
            transition: transform 0.3s ease;
        }

        .yt-modal-overlay.show .yt-download-modal {
            transform: scale(1);
        }

        .yt-modal-close {
            position: absolute;
            top: 1.2rem;
            right: 1.2rem;
            background: rgba(255, 255, 255, 0.1);
            border: none;
            color: white;
            font-size: 1.2rem;
            cursor: pointer;
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .yt-modal-close:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: rotate(90deg);
        }

        .yt-modal-header h3 {
            margin: 0 0 1rem 0;
            font-size: 1.8rem;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .yt-modal-content {
            margin: 1.5rem 0 2rem 0;
            padding: 1.5rem;
            background: rgba(255, 255, 255, 0.08);
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.12);
        }

        .yt-modal-content p {
            margin: 0.8rem 0;
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.9);
        }

        .yt-modal-content strong {
            color: #ffffff;
        }

        .yt-download-link {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 1.2rem 2.5rem;
            border: none;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .yt-download-link:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.6);
            text-decoration: none;
            color: white;
        }

        /* Responsive design for YouTube page */
        @media (max-width: 768px) {
            .yt-thumbnail-display img {
                max-height: 220px;
            }

            .yt-video-title {
                font-size: 1.3rem;
            }

            .yt-video-details {
                gap: 1rem;
                flex-direction: row;
            }

            .yt-format-strap {
                flex-direction: column;
                align-items: flex-start;
                gap: 1.2rem;
                padding: 1.5rem;
            }

            .yt-convert-button {
                align-self: stretch;
                justify-content: center;
                padding: 1rem 1.5rem;
            }

            .yt-format-type-quality {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }
        }

        @media (max-width: 480px) {
            .yt-media-preview-container {
                padding: 1.5rem;
                margin: 1.5rem 0;
            }

            .yt-modal-content {
                padding: 1rem;
            }

            .yt-download-modal {
                padding: 2rem;
            }
        }

        /* Force hide any non-YouTube UI elements that might appear */
        .media-preview:not(.yt-media-preview-container),
        .media-item:not(.yt-format-strap),
        .media-grid:not(.yt-format-options-list),
        .download-media-btn:not(.yt-convert-button):not(.yt-download-link),
        .media-thumbnail:not(.yt-thumbnail-display img),
        .media-count {
            display: none !important;
            visibility: hidden !important;
        }

        /* Ensure only YouTube-specific elements are visible in result container */
        #resultContent > *:not(.yt-media-preview-container) {
            display: none !important;
        }

        /* Force YouTube UI to always be visible when present */
        .yt-media-preview-container,
        .yt-thumbnail-display,
        .yt-video-info,
        .yt-format-options-list,
        .yt-format-strap,
        .yt-convert-button,
        .yt-reset-button {
            display: flex !important;
            visibility: visible !important;
        }

        .yt-media-preview-container {
            display: block !important;
        }

        .yt-format-options-list {
            display: flex !important;
            flex-direction: column !important;
        }
        
        /* YouTube Result Container Alignment Fixes */
        #resultContainer {
            display: none;
            width: 100%;
            max-width: 100%;
            margin: 0 auto;
            padding: 0;
            justify-content: center;
            align-items: flex-start;
        }
        
        #resultContainer.show {
            display: flex !important;
        }
        
        #resultContent {
            width: 100%;
            max-width: 900px;
            margin: 0 auto;
            padding: 0;
        }
        
        /* Ensure proper spacing and alignment */
        .yt-media-preview-container {
            display: block !important;
            width: 100%;
            max-width: 100%;
            margin: 2rem auto;
            position: relative;
        }
        
        /* Fix video header alignment */
        .yt-video-header {
            width: 100%;
            max-width: 100%;
        }
        
        /* Ensure thumbnail container is properly sized */
        .yt-thumbnail-display {
            min-width: 300px;
            max-width: 400px;
        }
        
        /* Fix video info alignment */
        .yt-video-info {
            min-width: 0;
            flex: 1;
        }
        
        /* Ensure format list takes full width */
        .yt-format-options-list {
            width: 100%;
            margin-top: 1rem;
        }
        
        /* Fix responsive layout */
        @media (max-width: 768px) {
            .yt-thumbnail-display {
                min-width: unset;
                max-width: 100%;
                width: 100%;
            }
            
            .yt-media-preview-container {
                margin: 1rem auto;
            }
        }
    </style>
</head>
<body>
    <div class="particles"></div>
    <div class="header">
        <a href="/" class="back-btn">
            <i class="fas fa-arrow-left"></i>
            Back to Home
        </a>
        <div class="platform-icon-large">
            <i class="fab fa-youtube"></i>
        </div>
        <h1 class="platform-title">YouTube Downloader</h1>
        <p class="platform-subtitle">Download high-quality content from YouTube with ease</p>
    </div>

    <div class="download-card">
        <form id="downloadForm" method="POST">
            <div class="input-section">
                <label for="urlInput" class="input-label">
                    <i class="fas fa-link"></i>
                    Enter YouTube URL
                </label>
                <div class="url-input-container">
                    <input type="text" id="urlInput" name="url" class="url-input" 
                           placeholder="Paste your YouTube link here..." autocomplete="off" />
                    <div class="input-actions">
                        <button type="button" class="action-btn" id="pasteBtn" title="Paste from clipboard">
                            <i class="fas fa-paste"></i>
                            Paste
                        </button>
                        <button type="button" class="action-btn" id="clearBtn" title="Clear input" style="display:none;">
                            <i class="fas fa-times"></i>
                            Clear
                        </button>
                    </div>
                </div>
                <div id="urlWarning" class="url-warning">
                    <i class="fas fa-exclamation-triangle"></i>
                    Not a valid YouTube URL. Please check and try again.
                </div>
            </div>
            <button type="submit" class="download-btn" id="downloadBtn">
                <i class="fas fa-download"></i>
                Fetch Video Info
            </button>
        </form>
    </div>

    <div class="loading-container" id="loadingIndicator">
        <div class="loading-spinner"></div>
        <div class="loading-text">Processing your request...</div>
    </div>

    <div class="result-container" id="resultContainer">
        <div class="result-content" id="resultContent"></div>
    </div>

    <script src="/js/common.js"></script>
    <script src="/js/youtube.js"></script>
</body>
</html>
